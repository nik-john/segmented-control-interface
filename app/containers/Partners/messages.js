/*
 * Partners Messages
 *
 * This contains all the text for the Partners container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Partners';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Partners Page',
  },
});
