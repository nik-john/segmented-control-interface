/*
 * Audiences Messages
 *
 * This contains all the text for the Audiences container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Audiences';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Audiences Page',
  },
});
