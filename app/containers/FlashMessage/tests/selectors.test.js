// import { selectFlashMessageDomain } from '../selectors';

describe('selectFlashMessageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
